
<%- if current_user.nil? %>
	<div class="yourFeedback login">
		<h2>Add a Missing Ballot Measure or Candidate</h2>
		<p>You must be logged in through Facebook to use this feature</p>
		<%= link_to (image_tag 'fb-login.png'), omniauth_authorize_path('user', :facebook)  %>
	</div>
<% else %>
	<%= render :partial => ['choice',params[:type],'newForm'].join('/') %>
	<div class="add-warning">
	<p>Warning: Once you create this contest you will not be able to edit it. So make sure it's perfect. Be sure to include all candidates running for the office. It will appear on your <a href="<%= current_user.profile %>">voter guide</a> and on the appropriate State page under the section for "User Created" contests.</p>
	<p>Ballot Measures and Candidates that are fictitious (not really on the 2012 ballot), duplicates of existing measures or candidates, or deemed misleading will be removed without notice.</p>
	</div>
	<script type="text/javascript">
	$(document).on('submit','#new_choice', function(e) { 
		var success = true
		$('.required',this).each( function() { 
			var $this = $(this)
			if( $this.val().length < 1 )  {
				success = success && false
				$this.parent().addClass('oops')
			}
			
		})
		
		if( success ) return confirm('Are you sure you want to create this?')
		else return false;
	})
	</script>
<% end %>